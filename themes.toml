# Each theme's docs live in their own repository, published at /<slug>/.
# Order here is the order they appear on the home page.
#
# Nothing is listed any more: every docs repo here has moved to its own host —
# docs.owldraft.com, docs.heronwp.com, and fox-templates.heronwp.com for the
# Fox template directory — and the repos here now serve forwards to them. The
# list only feeds the sitemap lines in robots.txt, and a forward has no
# sitemap, so it stays empty.
